# Ignore all
*
# Unignore all with extensions
# mac에서는 gcc 컴파일 후 확장자 없는 bin 파일이 생성됨
!*.*
# Unignore all dirs
!*/
# mac에서는 gcc 컴파일 후 확장자 없는 .dSYM 폴더가 생성됨
*.dSYM*
.vscode
*.o
*.exe
\*/.DS_Store/
.DS_Store/