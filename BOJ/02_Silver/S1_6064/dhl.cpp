#include <iostream>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("input.txt", "r", stdin);

    int tc;
    cin >> tc;

    while (tc--){
        int m, n, tgt_x, tgt_y;
        cin >> m >> n >> tgt_x >> tgt_y;

        int year = 1;
        int x = 1; int y = 1;
        
        for (int k = 0; k < m; k += m){
            
        }

        cout << year << '\n';
    }

    return 0;
}